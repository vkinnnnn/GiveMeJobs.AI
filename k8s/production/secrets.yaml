# This is a template file - actual secrets should be created using kubectl or sealed-secrets
apiVersion: v1
kind: Secret
metadata:
  name: givemejobs-secrets
  namespace: givemejobs-prod
  labels:
    app.kubernetes.io/name: givemejobs
    app.kubernetes.io/component: secrets
type: Opaque
stringData:
  # Database Credentials (replace with actual values)
  POSTGRES_USER: "givemejobs"
  POSTGRES_PASSWORD: "REPLACE_WITH_ACTUAL_PASSWORD"
  MONGO_USER: "givemejobs"
  MONGO_PASSWORD: "REPLACE_WITH_ACTUAL_PASSWORD"
  REDIS_PASSWORD: "REPLACE_WITH_ACTUAL_PASSWORD"
  
  # JWT Secrets
  JWT_SECRET: "REPLACE_WITH_ACTUAL_JWT_SECRET"
  JWT_REFRESH_SECRET: "REPLACE_WITH_ACTUAL_JWT_REFRESH_SECRET"
  
  # External API Keys
  OPENAI_API_KEY: "REPLACE_WITH_ACTUAL_OPENAI_KEY"
  PINECONE_API_KEY: "REPLACE_WITH_ACTUAL_PINECONE_KEY"
  LINKEDIN_CLIENT_ID: "REPLACE_WITH_ACTUAL_LINKEDIN_CLIENT_ID"
  LINKEDIN_CLIENT_SECRET: "REPLACE_WITH_ACTUAL_LINKEDIN_CLIENT_SECRET"
  INDEED_API_KEY: "REPLACE_WITH_ACTUAL_INDEED_KEY"
  
  # Monitoring
  SENTRY_DSN: "REPLACE_WITH_ACTUAL_SENTRY_DSN"
  
  # Email Service
  RESEND_API_KEY: "REPLACE_WITH_ACTUAL_RESEND_KEY"
  
  # Encryption Keys
  ENCRYPTION_KEY: "REPLACE_WITH_ACTUAL_ENCRYPTION_KEY"
  
---
# TLS Certificate Secret (replace with actual certificate)
apiVersion: v1
kind: Secret
metadata:
  name: tls-secret
  namespace: givemejobs-prod
type: kubernetes.io/tls
data:
  tls.crt: LS0tLS1CRUdJTi... # Base64 encoded certificate
  tls.key: LS0tLS1CRUdJTi... # Base64 encoded private key