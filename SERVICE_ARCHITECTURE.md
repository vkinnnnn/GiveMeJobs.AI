# Service Architecture Overview

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        GiveMeJobs Platform                       │
└─────────────────────────────────────────────────────────────────┘
                                 │
                ┌────────────────┼────────────────┐
                │                │                │
                ▼                ▼                ▼
        ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
        │   Frontend   │ │   Backend    │ │  Databases   │
        │  (Next.js)   │ │  (Express)   │ │              │
        └──────────────┘ └──────────────┘ └──────────────┘
                                 │
                ┌────────────────┼────────────────┐
                │                │                │
                ▼                ▼                ▼
        ┌──────────────┐ ┌──────────────┐ ┌──────────────┐
        │ Core Services│ │ External APIs│ │  Optional    │
        │  (Required)  │ │ (Recommended)│ │  Services    │
        └──────────────┘ └──────────────┘ └──────────────┘
```

## Service Breakdown

### ✅ Core Services (Already Configured)

```
┌─────────────────────────────────────────────────────────┐
│                    Core Services                         │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐ │
│  │  PostgreSQL  │  │   MongoDB    │  │    Redis     │ │
│  │              │  │              │  │              │ │
│  │  User Data   │  │  Documents   │  │   Sessions   │ │
│  │  Jobs        │  │  Resumes     │  │   Cache      │ │
│  │  Applications│  │  Templates   │  │   Rate Limit │ │
│  └──────────────┘  └──────────────┘  └──────────────┘ │
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │              JWT Authentication                   │  │
│  │  - Access tokens (15 min)                        │  │
│  │  - Refresh tokens (7 days)                       │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### ⚠️ External APIs (Need Configuration)

```
┌─────────────────────────────────────────────────────────┐
│                  Authentication                          │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────┐              ┌──────────────┐        │
│  │    Google    │              │   LinkedIn   │        │
│  │    OAuth     │              │    OAuth     │        │
│  │              │              │              │        │
│  │  Social Login│              │ Social Login │        │
│  │  Profile Sync│              │ Profile Sync │        │
│  └──────────────┘              └──────────────┘        │
│                                                          │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                  Communication                           │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │                   SendGrid                        │  │
│  │                                                   │  │
│  │  - Welcome emails                                │  │
│  │  - Password reset                                │  │
│  │  - Job alerts                                    │  │
│  │  - Interview reminders                           │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
│  Note: Development mode uses Ethereal (no config)       │
│                                                          │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                    AI Services                           │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │                    OpenAI                         │  │
│  │                                                   │  │
│  │  - Resume generation (GPT-4)                     │  │
│  │  - Cover letter generation                       │  │
│  │  - Interview preparation                         │  │
│  │  - Job description analysis                      │  │
│  │  - Text embeddings (ada-002)                     │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

### 📦 Optional Services

```
┌─────────────────────────────────────────────────────────┐
│                  Vector Database                         │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────────────────────────────────────────┐  │
│  │                   Pinecone                        │  │
│  │                                                   │  │
│  │  - Job embeddings storage                        │  │
│  │  - Semantic job search                           │  │
│  │  - Job matching recommendations                  │  │
│  └──────────────────────────────────────────────────┘  │
│                                                          │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│                   Job Board APIs                         │
├─────────────────────────────────────────────────────────┤
│                                                          │
│  ┌──────────────┐              ┌──────────────┐        │
│  │    Indeed    │              │  Glassdoor   │        │
│  │     API      │              │     API      │        │
│  │              │              │              │        │
│  │  Job Search  │              │  Job Search  │        │
│  │  Job Details │              │  Company Info│        │
│  └──────────────┘              └──────────────┘        │
│                                                          │
└─────────────────────────────────────────────────────────┘
```

## Data Flow

### User Registration Flow

```
User → Frontend → Backend → PostgreSQL
                    │
                    ├─→ MongoDB (Profile)
                    ├─→ Redis (Session)
                    └─→ SendGrid (Welcome Email)
```

### OAuth Login Flow

```
User → Frontend → Google/LinkedIn → Backend → PostgreSQL
                                       │
                                       ├─→ MongoDB (Profile)
                                       ├─→ Redis (Session)
                                       └─→ JWT Tokens
```

### Resume Generation Flow

```
User → Frontend → Backend → OpenAI (GPT-4)
                    │
                    ├─→ MongoDB (Save Resume)
                    └─→ Frontend (Display)
```

### Job Matching Flow

```
User Profile → Backend → OpenAI (Embeddings)
                  │
                  ├─→ Pinecone (Vector Search)
                  ├─→ PostgreSQL (Job Details)
                  └─→ Frontend (Recommendations)
```

### Job Alert Flow

```
Scheduler → Backend → PostgreSQL (Get Alerts)
              │
              ├─→ Indeed/Glassdoor (Search Jobs)
              ├─→ PostgreSQL (Save Jobs)
              ├─→ SendGrid (Email Alert)
              └─→ WebSocket (Real-time Notification)
```

## Service Dependencies

```
┌─────────────────────────────────────────────────────────┐
│                  Dependency Tree                         │
└─────────────────────────────────────────────────────────┘

Backend API
├── PostgreSQL (Required)
│   └── User data, jobs, applications
│
├── MongoDB (Required)
│   └── Documents, resumes, templates
│
├── Redis (Required)
│   └── Sessions, cache, rate limiting
│
├── JWT (Required)
│   └── Authentication tokens
│
├── Google OAuth (Optional)
│   └── Social login
│
├── LinkedIn OAuth (Optional)
│   └── Social login + profile import
│
├── SendGrid (Optional - has fallback)
│   └── Production emails
│   └── Fallback: Ethereal (dev mode)
│
├── OpenAI (Optional)
│   ├── GPT-4 (text generation)
│   └── Ada-002 (embeddings)
│       └── Pinecone (Optional)
│           └── Vector storage
│
└── Job Board APIs (Optional)
    ├── Indeed API
    └── Glassdoor API
```

## Configuration Priority

```
Priority 1 (Required - Already Done ✅)
├── PostgreSQL
├── MongoDB
├── Redis
└── JWT

Priority 2 (Recommended - Needs Config ⚠️)
├── Google OAuth (social login)
├── SendGrid (production emails)
└── OpenAI (AI features)

Priority 3 (Optional - Can Wait 📦)
├── LinkedIn OAuth
├── Pinecone
├── Indeed API
└── Glassdoor API
```

## Service Status Check

Run this to see your current configuration:

```bash
cd packages/backend
npm run check:all
```

Output will show:
```
✅ Configured and working
⚠️  Not configured (optional)
❌ Not configured (required)
```

## Testing Services

```bash
# Test individual services
npm run test:oauth      # OAuth configuration
npm run test:email      # Email service
npm run test:openai     # OpenAI API
npm run test:pinecone   # Pinecone vector DB
npm run redis:test      # Redis cache

# Test multiple services
npm run test:services   # OAuth + Email + Redis

# Check all services
npm run check:all       # Comprehensive status
```

## Next Steps

1. **Check current status:**
   ```bash
   npm run check:all
   ```

2. **Configure priority services:**
   - Google OAuth (5 min)
   - SendGrid (5 min)
   - OpenAI (5 min)

3. **Test configuration:**
   ```bash
   npm run test:services
   ```

4. **Start application:**
   ```bash
   npm run dev
   ```

---

For detailed setup instructions, see:
- `START_HERE.md` - Quick start
- `QUICK_SERVICE_SETUP.md` - Quick reference
- `SERVICE_CONFIGURATION_GUIDE.md` - Detailed guide
